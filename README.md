# React App Skeleton Builder  
Build your react-app completely from docker without any dependencies on your local machine.

## Instructions  

```bash 
chmod +x build.sh  
./build.sh
```  

Edit package.json in order to choose packages version (you can also use the default without editing)   

```bash  
docker-compose up
```  

react-app will be hosted on localhost:3000
